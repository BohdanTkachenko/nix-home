# Copy to .env (gitignored). Customer Secret Key for OCI Object Storage's
# S3-compatible API — used by the OpenTofu `s3` backend.
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
